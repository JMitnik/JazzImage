const AppTheme = {
  colors: {}
};

AppTheme.colors.primary = '#d4fefe';
AppTheme.colors.secondary = '#444';
AppTheme.colors.white = 'white';
AppTheme.colors.black = '#030303';

AppTheme.colors.body = AppTheme.colors.black;

export default AppTheme;

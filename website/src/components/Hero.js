import React from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

const HeroStyles = styled.section`
    min-height: 100vh;
`;

const Hero = props => <HeroStyles />;

Hero.propTypes = {};

export default Hero;
